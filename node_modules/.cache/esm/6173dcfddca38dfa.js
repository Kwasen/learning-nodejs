let clg,convertSize,Logger;_dca‍.w("./utils.js",[["clg",["clg"],function(v){clg=v}],["convertSize",["convertSize"],function(v){convertSize=v}],["Logger",["Logger"],function(v){Logger=v}]]);const http = require('http');
const os = require('os');
const fs = require('fs');
const EventEmitter = require('events');


const logger = new Logger();

// var totalMemory = os.totalmem();
// var freeMemory = os.freemem();
// console.log('Total Memory: ', convertSize(totalMemory, 'gb'), 'Free Memory: ', convertSize(freeMemory, 'gb'));

// clg('---------------------');
// const files = fs.readdirSync('./');
// clg(files);

// clg('---------------------');
// fs.readdir('./sda', (err, files) => {
//	if (err) clg(err);
//	else clg(files);
//})
// clg('------------------------');
// // Register a listener
// logger.on('logged', (eventArg) => {
//	// clg.(eventArg);
//	console.log(eventArg);
// });

//logger.log('Sample message here');

const server = http.createServer();
servier.on('connection', (socket) => {
	clg('New connection');
	clg(socket);
});

server.listen(3000);
clg('Listening on port 3000...');
